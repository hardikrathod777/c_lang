#include <stdio.h>
main() {
    int n, a, sum;
    printf("Enter a value you want : ");
    scanf("%d", &n);
    a = 1;

    for(sum=0,a,n; a <= n; sum+=a,++a) {
        
    }

    printf("Sum = %d", sum);
}
